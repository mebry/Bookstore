@model string;

<div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <div class="card border-success">
            <div class="card-body text-center">
                <h1 class="text-success mb-4">Success!</h1>
                <p class="lead mb-4">Your operation was successful</p>
                <a class="btn btn-success" href="@Url.Action("Index", @Model)">Go to Home Page</a>
            </div>
        </div>
    </div>
</div>